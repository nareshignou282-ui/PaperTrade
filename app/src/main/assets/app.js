const A=document.querySelector('#app'), S=document.querySelector('#sheetRoot');
const fmt=n=>'₹'+Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:2,minimumFractionDigits:2});
const money=n=>Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:2,minimumFractionDigits:2});
const now=()=>new Date().toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit'});
const state=JSON.parse(localStorage.getItem('pt4')||'null')||{
  page:'home',top:'Home',cash:1000000,used:0,realized:0,kill:false,
  quotes:{NIFTY:{p:24126.5,c:-58,pc:-.24},SENSEX:{p:76851.7,c:-105.57,pc:-.14},VIX:{p:11.22,c:.03,pc:.27},RELIANCE:{p:1490.56,c:11.8,pc:.8},HDFCBANK:{p:1990.42,c:-5.2,pc:-.26},INFY:{p:1515.97,c:19.1,pc:1.27},TCS:{p:3146.42,c:-14.1,pc:-.45}},
  positions:[],orders:[],diary:[],watch:['NIFTY','SENSEX','VIX','RELIANCE','HDFCBANK','INFY','TCS'],
  limits:{loss:5000,trades:20,maxPos:6},
  provider:'hybrid'
};
function save(){localStorage.setItem('pt4',JSON.stringify(state))}
function icon(i){return i}
function topbar(titleTabs,active){return `<div class="top"><div class="head"><div class="avatar">P</div><div class="hello">Hello <b>PAPER TRADER</b></div><div class="headicons"><span>◉</span><span>♢</span><span>⌕</span></div></div>${titleTabs?`<div class="tabs">${titleTabs.map(x=>`<div class="tab ${x===active?'on':''}" onclick="topTab('${x}')">${x}</div>`).join('')}</div>`:''}</div>`}
function bottom(){return `<div class="bottom">
<div class="nav ${state.page==='home'?'on':''}" onclick="go('home')"><div class="ni">⌂</div><div>Home</div></div>
<div class="nav ${state.page==='watch'?'on':''}" onclick="go('watch')"><div class="ni">☆</div><div>Watchlist</div></div>
<div class="nav center ${state.page==='portfolio'?'on':''}" onclick="go('portfolio')"><div class="centerLogo">P</div><div>Portfolio</div></div>
<div class="nav ${state.page==='orders'?'on':''}" onclick="go('orders')"><div class="ni">☷</div><div>Orders</div></div>
<div class="nav ${state.page==='funds'?'on':''}" onclick="go('funds')"><div class="ni">♙</div><div>Funds</div></div></div>`}
function go(p){state.page=p;if(p==='home')state.top='Home';if(p==='orders')state.top='Positions';if(p==='funds')state.top='Funds';save();render();scrollTo(0,0)}
function topTab(t){state.top=t;if(['Stocks','Options'].includes(t)&&state.page==='home'){ if(t==='Options') state.page='options'; else state.page='home';} if(state.page==='orders')state.top=t;if(state.page==='funds')state.top=t;save();render();}
function liveTicker(){return `<div class="livebar"><div class="tv" id="ticker"></div></div>`}
function injectTicker(){const el=document.querySelector('#ticker');if(!el)return;el.innerHTML=`<div class="tradingview-widget-container" style="height:36px"><div class="tradingview-widget-container__widget"></div></div>`;const s=document.createElement('script');s.src='https://s3.tradingview.com/external-embedding/embed-widget-ticker-tape.js';s.async=true;s.innerHTML=JSON.stringify({symbols:[{proName:'NSE:NIFTY',title:'NIFTY 50'},{proName:'BSE:SENSEX',title:'SENSEX'},{proName:'NSE:RELIANCE',title:'RELIANCE'},{proName:'NSE:TCS',title:'TCS'}],showSymbolLogo:false,isTransparent:true,displayMode:'compact',colorTheme:'light',locale:'en'});el.firstElementChild.appendChild(s)}
function q(sym){return state.quotes[sym]||{p:0,c:0,pc:0}}
function qrow(sym,sub='NSE · EQ'){const x=q(sym);return `<div class="market-card row between" onclick="openSymbol('${sym}')"><div><div class="symbol">${sym==='VIX'?'India VIX':sym}</div><div class="exchange">${sub}</div></div><div><div class="price">${money(x.p)}</div><div class="chg ${x.pc>=0?'green':'red'}">${x.c>=0?'+':''}${money(x.c)} (${x.pc>=0?'+':''}${x.pc.toFixed(2)}%) ${x.pc>=0?'↗':'↘'}</div></div></div>`}
function home(){const available=state.cash-state.used;return `<div class="app">${topbar(['Home','Stocks','Options','Mutual Funds','Community'],state.top)}${liveTicker()}<main class="content">
<div class="card pad"><div class="row between"><span class="green">● Paper market</span><span class="badge">VIRTUAL</span></div><div style="margin-top:8px;font-size:12px;color:#777">Trade live-market style with virtual capital. No real orders are sent.</div></div>
<div class="card hero"><div class="row between"><div class="portfolio-title">Your Paper Portfolio</div><span class="muted tiny">Overall</span></div><div class="profit">${fmt(totalPnl())} <span class="tiny ${totalPnl()>=0?'green':'red'}">${totalPnl()>=0?'PROFIT':'LOSS'}</span></div><div class="split"><div class="metric"><span class="muted tiny">Virtual Investment</span><br><b>${fmt(state.used)}</b></div><div class="metric"><span class="muted tiny">Available Capital</span><br><b>${fmt(available)}</b></div></div></div>
<div class="section-title">Invest / Trade on Stock Exchanges</div><div class="card grid4"><div onclick="go('watch')"><div class="quickIcon">◔</div><div class="quickLabel">Stocks</div></div><div onclick="go('options')"><div class="quickIcon">▦</div><div class="quickLabel">Options</div></div><div onclick="go('orders')"><div class="quickIcon">📊</div><div class="quickLabel">Futures</div></div><div><div class="quickIcon">◒</div><div class="quickLabel">Commodities</div></div></div>
<div class="section-title">Live Markets</div><div class="card">${qrow('NIFTY','NSE · INDEX')}${qrow('SENSEX','BSE · INDEX')}${qrow('VIX','NSE · INDEX')}</div>
<div class="section-title">Quick Access</div><div class="quicktiles"><div class="qt" onclick="flash()"><div class="ico">⚡</div><b>Scalper</b><span>Fast paper execution</span></div><div class="qt" onclick="chart('NSE:NIFTY')"><div class="ico">📈</div><b>Charts</b><span>TradingView chart</span></div><div class="qt" onclick="go('options')"><div class="ico">⌁</div><b>Flash</b><span>ATM options</span></div></div>
<div class="section-title">Trending on PaperTrade</div><div class="card">${qrow('RELIANCE')}${qrow('HDFCBANK')}${qrow('INFY')}${qrow('TCS')}</div>
<div class="soft-banner">PaperTrade • learn, test, improve — without risking real money.</div><div class="footerfade">you are made for paper trade</div></main>${bottom()}</div>`}
function watch(){return `<div class="app">${topbar(['Watchlist','News','CA','Trending'],'Watchlist')}<main class="content"><div class="search">⌕ <span class="muted">Search companies, indices or options</span></div><div class="filterbar"><div class="filter on">all</div><div class="filter">FnO Stocks</div><div class="filter">FnO</div><div class="filter">⚙</div></div><div class="card">${state.watch.map((s,i)=>{const x=q(s);return `<div class="watchrow" onclick="openSymbol('${s}')"><div class="row between"><div><div class="name">${s==='VIX'?'India VIX':s}</div><div class="muted tiny">${['SENSEX'].includes(s)?'BSE':'NSE'}</div></div><div><div class="price">${money(x.p)}</div><div class="${x.pc>=0?'green':'red'}">${x.c>=0?'+':''}${money(x.c)} (${x.pc>=0?'+':''}${x.pc.toFixed(2)}%) ${x.pc>=0?'↗':'↘'}</div></div></div><div class="added">${i<3?`${x.pc>=0?'↗':'↘'} ${Math.abs(x.pc*5).toFixed(2)}% since added`:'Tap for chart / paper order'}</div></div>`}).join('')}</div><div class="footerfade">track markets • build discipline</div></main>${bottom()}</div>`}
function portfolio(){return `<div class="app">${topbar(['All','Stocks','ETF','Options','Positions'],'All')}<main class="content"><div class="card pad"><div class="muted tiny">My Virtual Net Worth</div><div class="profit">${fmt(state.cash+totalPnl())}</div><div class="row between"><span class="tiny">Paper account</span><span class="green tiny">● LIVE UI</span></div></div><div class="section-title">Investments on PaperTrade</div><div class="card"><div class="portfolio-card"><div class="row between"><span class="port-title">📈 Stocks</span><span>›</span></div><div class="port-val">${fmt(stockValue())}</div><div class="muted tiny">Virtual equity positions</div></div><div class="portfolio-card"><div class="row between"><span class="port-title">▦ Options</span><span>›</span></div><div class="port-val">${fmt(optionValue())}</div><div class="muted tiny">NIFTY option positions</div></div><div class="portfolio-card"><div class="row between"><span class="port-title">📍 Positions</span><span>›</span></div><div class="port-val">${state.positions.length}</div><div class="muted tiny">Open paper positions</div></div></div><div class="section-title">Open positions</div><div class="card">${positionsHtml()||'<div class="empty" style="padding:35px"><div class="art">☷</div><h2>No Positions</h2><div>Place a paper order to begin.</div></div>'}</div><div class="footerfade">build your skill on PaperTrade</div></main>${bottom()}</div>`}
function orders(){let body='';if(state.top==='Positions')body=positionsHtml()||empty('No Positions Today','P&L for your open paper positions will appear here');else if(state.top==='Orders')body=ordersHtml()||empty('No Orders Today','Your paper order history will appear here');else if(state.top==='Super')body=`<div class="card pad"><b>Super Order</b><p class="muted">Place Entry + Target + Stop Loss together. Both exit legs are virtual OCO legs.</p><button class="btn greenBtn" onclick="orderSheet('NIFTY','BUY','SUPER')">+ New Super Order</button></div>`;else body=`<div class="card pad"><b>Flash / Scalper</b><p class="muted">One-tap paper trading around NIFTY ATM options.</p><button class="btn greenBtn" onclick="flash()">Open Flash Trade</button></div>`;return `<div class="app">${topbar(['Positions','Orders','Super','Flash'],state.top)}<main class="content"><div class="card pad"><span class="muted">Overall P&L</span><div class="profit">${fmt(totalPnl())} <span class="tiny">on ${state.positions.length} positions</span></div></div>${body}</main>${bottom()}</div>`}
function funds(){if(state.top==='Trader’s Diary')return diary();if(state.top==='Controls')return controls();return `<div class="app">${topbar(['Funds','Trader’s Diary','Controls'],state.top)}<main class="content"><div class="card fundHero"><div class="muted">Available for Investing</div><div class="big">${fmt(state.cash-state.used)}</div><div class="amount"><input id="addMoney" type="number" value="100000"><button class="btn greenBtn" onclick="topup()">Top-Up Virtual</button></div></div><div class="section-title">Real-time Margin Utilization</div><div class="card pad"><div class="split"><div><span class="muted">Margin Available</span><div class="profit" style="font-size:20px">${fmt(state.cash-state.used)}</div></div><div><span class="muted">Margin Used</span><div class="profit" style="font-size:20px">${fmt(state.used)}</div></div></div><div class="row between"><span>Utilization</span><b>${((state.used/state.cash)*100||0).toFixed(2)}%</b></div><div style="height:8px;background:#eee;border-radius:5px;margin:10px 0"><div style="height:8px;width:${Math.min(100,(state.used/state.cash)*100||0)}%;background:var(--green);border-radius:5px"></div></div><div class="listline"><span>Cash Available</span><b>${fmt(state.cash-state.used)}</b></div><div class="listline"><span>Realized P&L</span><b class="${state.realized>=0?'green':'red'}">${fmt(state.realized)}</b></div></div><div class="section-title">Manage Virtual Funds</div><div class="card"><div class="listline">All Transactions <span>›</span></div><div class="listline">Reset Paper Account <span onclick="resetAccount()">›</span></div><div class="listline">Export Trade Ledger <span>›</span></div></div><div class="dataNote">Virtual funds have no monetary value. PaperTrade never deposits, withdraws, or sends orders to a brokerage account.</div><div class="footerfade">practice to grow your process</div></main>${bottom()}</div>`}
function options(){return `<div class="app">${topbar(['Option Chain','Strategies','Flash','Charts'],'Option Chain')}<main class="content"><div class="optionsTop"><div class="row between"><div><div class="underlying">NIFTY 50</div><div class="muted tiny">NSE · Index</div></div><div style="text-align:right"><div class="spot">${money(q('NIFTY').p)}</div><div class="red tiny">${money(q('NIFTY').c)} (${q('NIFTY').pc.toFixed(2)}%)</div></div></div><div class="expiry"><button class="on">03 SEP</button><button>10 SEP</button><button>17 SEP</button><button>24 SEP</button><button>MONTHLY</button></div><div class="row between" style="margin-top:10px"><span class="tiny">PCR <b>0.94</b> · Max Pain <b>${atm()}</b></span><span class="badge">FULL CHAIN</span></div></div><div class="chainWrap"><div class="chainHead"><div>CALLS · LTP / OI</div><div>STRIKE</div><div>PUTS · LTP / OI</div></div>${chainRows()}</div><div class="dataNote">TradingView powers chart views. Option-chain values in this build use the PaperTrade provider adapter; connect an authorized live options feed for exchange-grade raw execution data.</div></main>${bottom()}</div>`}
function chainRows(){const a=atm(),rows=[];for(let strike=a-1250;strike<=a+1250;strike+=50){const d=(strike-q('NIFTY').p)/50;const intrinsicC=Math.max(0,q('NIFTY').p-strike);const intrinsicP=Math.max(0,strike-q('NIFTY').p);const time=Math.max(3,155-Math.abs(d)*5);const ce=Math.max(1,intrinsicC+time+Math.sin(strike)*4);const pe=Math.max(1,intrinsicP+time-Math.sin(strike)*4);const oiC=Math.round(350000+Math.abs(d)*24000+(strike%200)*1900),oiP=Math.round(380000+Math.abs(d)*22000+((strike+50)%200)*2100);rows.push(`<div class="chainRow ${strike===a?'atm':''}"><div class="optSide call buytap" onclick="orderSheet('NIFTY ${strike} CE','BUY','NRML',${ce.toFixed(2)})"><div><div class="ltp">${ce.toFixed(2)}</div><div class="sub">OI ${(oiC/100000).toFixed(1)}L</div></div><div><div class="sub">IV ${(12+Math.abs(d)*.18).toFixed(1)}%</div><div class="miniLabel green">BUY</div></div></div><div class="strike">${strike}${strike===a?'<span class="miniLabel">ATM</span>':''}</div><div class="optSide put buytap" onclick="orderSheet('NIFTY ${strike} PE','BUY','NRML',${pe.toFixed(2)})"><div><div class="sub">IV ${(12.2+Math.abs(d)*.17).toFixed(1)}%</div><div class="miniLabel green">BUY</div></div><div><div class="ltp">${pe.toFixed(2)}</div><div class="sub">OI ${(oiP/100000).toFixed(1)}L</div></div></div></div>`)}return rows.join('')}
function atm(){return Math.round(q('NIFTY').p/50)*50}
function chart(symbol){state.chartSymbol=symbol;state.page='chart';save();render()}
function chartPage(){const sym=state.chartSymbol||'NSE:NIFTY';return `<div class="app">${topbar(['Chart','Trade'],'Chart')}<main class="content"><div class="card pad row between"><div><b>${sym.replace('NSE:','')}</b><div class="tiny muted">TradingView embedded chart</div></div><button class="btn greenBtn" onclick="orderSheet('${sym.replace('NSE:','')}','BUY','MIS')">Trade</button></div><div class="tvbox" id="tvchart"></div><div class="dataNote">The chart is an official TradingView embed. PaperTrade does not scrape TradingView values into its execution engine.</div></main>${bottom()}</div>`}
function injectChart(){const el=document.querySelector('#tvchart');if(!el)return;const s=document.createElement('script');s.src='https://s3.tradingview.com/external-embedding/embed-widget-advanced-chart.js';s.async=true;s.innerHTML=JSON.stringify({autosize:true,symbol:state.chartSymbol||'NSE:NIFTY',interval:'5',timezone:'Asia/Kolkata',theme:'light',style:'1',locale:'en',allow_symbol_change:true,calendar:false,support_host:'https://www.tradingview.com'});el.appendChild(s)}
function openSymbol(sym){if(sym==='NIFTY')chart('NSE:NIFTY');else if(sym==='SENSEX')chart('BSE:SENSEX');else if(sym==='VIX')chart('NSE:INDIAVIX');else chart('NSE:'+sym)}
function flash(){const a=atm();S.innerHTML=`<div class="sheetback" onclick="if(event.target===this)closeSheet()"><div class="sheet"><div class="handle"></div><div class="row between"><div><b>Flash Trade · NIFTY</b><div class="tiny muted">ATM ${a}</div></div><span onclick="closeSheet()">✕</span></div><div class="split" style="margin-top:14px"><button class="place" onclick="orderSheet('NIFTY ${a} CE','BUY','NRML',155)">BUY ${a} CE</button><button class="place sell" onclick="orderSheet('NIFTY ${a} PE','BUY','NRML',152)">BUY ${a} PE</button></div><div class="card pad" style="margin-top:14px"><div class="row between"><span>Spot</span><b>${money(q('NIFTY').p)}</b></div><div class="row between" style="margin-top:8px"><span>Default SL</span><b>20%</b></div><div class="row between" style="margin-top:8px"><span>Default Target</span><b>30%</b></div></div></div></div>`}
function orderSheet(symbol,side='BUY',product='MIS',price){if(state.kill){toast('Kill Switch is ON. New paper orders are blocked.');return}const p=price||quoteFor(symbol);S.innerHTML=`<div class="sheetback" onclick="if(event.target===this)closeSheet()"><div class="sheet"><div class="handle"></div><div class="row between"><div><b>${symbol}</b><div class="tiny muted">Paper order · ${money(p)}</div></div><span onclick="closeSheet()">✕</span></div><div class="sideSwitch" style="margin-top:12px"><button id="buySide" class="buy ${side==='BUY'?'on':''}" onclick="sheetSide('BUY')">BUY</button><button id="sellSide" class="sell ${side==='SELL'?'on':''}" onclick="sheetSide('SELL')">SELL</button></div><div class="formgrid"><div class="field"><label>Product</label><select id="prod"><option ${product==='MIS'?'selected':''}>MIS</option><option ${product==='NRML'?'selected':''}>NRML</option><option>CNC</option><option>MTF</option></select></div><div class="field"><label>Order type</label><select id="otype"><option>MARKET</option><option>LIMIT</option><option>SL</option><option>SL-M</option><option ${product==='SUPER'?'selected':''}>SUPER</option></select></div><div class="field"><label>Quantity</label><input id="qty" type="number" value="${symbol.includes('NIFTY ') ? 75 : 1}"></div><div class="field"><label>Price</label><input id="oprice" type="number" value="${Number(p).toFixed(2)}"></div><div class="field"><label>Stop Loss</label><input id="sl" type="number" placeholder="Optional"></div><div class="field"><label>Target</label><input id="target" type="number" placeholder="Optional"></div></div><button id="placeBtn" class="place ${side==='SELL'?'sell':''}" data-side="${side}" data-symbol="${symbol}" onclick="placeFromSheet()">${side} · PAPER ORDER</button><div class="dataNote">No real brokerage order is placed. Estimated virtual margin: <b>${fmt(p*(symbol.includes('NIFTY ')?75:1))}</b></div></div></div>`}
function sheetSide(side){const b=document.querySelector('#buySide'),s=document.querySelector('#sellSide'),p=document.querySelector('#placeBtn');b.classList.toggle('on',side==='BUY');s.classList.toggle('on',side==='SELL');p.dataset.side=side;p.textContent=side+' · PAPER ORDER';p.classList.toggle('sell',side==='SELL')}
function placeFromSheet(){const b=document.querySelector('#placeBtn'),symbol=b.dataset.symbol,side=b.dataset.side,qty=Math.max(1,Number(document.querySelector('#qty').value)),price=Number(document.querySelector('#oprice').value),type=document.querySelector('#otype').value,prod=document.querySelector('#prod').value,sl=Number(document.querySelector('#sl').value)||null,target=Number(document.querySelector('#target').value)||null;const margin=Math.abs(price*qty);if(side==='BUY'&&margin>state.cash-state.used){toast('Insufficient virtual funds');return}const order={id:Date.now(),symbol,side,qty,price,type,prod,sl,target,status:'TRADED',time:now()};state.orders.unshift(order);state.diary.unshift({time:order.time,text:`${side} ${qty} ${symbol} @ ${price.toFixed(2)}`,tag:type});addPosition(order);save();closeSheet();toast(`${side} paper order filled at ${price.toFixed(2)}`);render()}
function addPosition(o){let pos=state.positions.find(p=>p.symbol===o.symbol);const signed=o.side==='BUY'?o.qty:-o.qty;if(!pos){pos={symbol:o.symbol,qty:signed,avg:o.price,ltp:o.price,prod:o.prod};state.positions.unshift(pos)}else{const old=pos.qty,newq=old+signed;if(old===0||Math.sign(old)===Math.sign(signed)){pos.avg=(Math.abs(old)*pos.avg+Math.abs(signed)*o.price)/(Math.abs(old)+Math.abs(signed));pos.qty=newq}else{const closed=Math.min(Math.abs(old),Math.abs(signed));const pnl=(o.price-pos.avg)*closed*Math.sign(old);state.realized+=pnl;pos.qty=newq;if(newq===0){state.positions=state.positions.filter(x=>x!==pos);pos=null}else if(Math.sign(newq)!==Math.sign(old)){pos.avg=o.price}}}if(o.side==='BUY')state.used+=Math.min(state.cash-state.used,Math.abs(o.price*o.qty));else state.used=Math.max(0,state.used-Math.abs(o.price*o.qty)*.5)}
function positionsHtml(){return state.positions.map((p,i)=>{p.ltp=liveLtp(p);const pnl=(p.ltp-p.avg)*p.qty;return `<div class="market-card"><div class="row between"><div><div class="symbol">${p.symbol}</div><div class="exchange">${p.prod} · Qty ${p.qty} · Avg ${p.avg.toFixed(2)}</div></div><div><div class="price">${p.ltp.toFixed(2)}</div><div class="chg ${pnl>=0?'green':'red'}">${fmt(pnl)}</div></div></div><div class="chips" style="margin-top:10px"><div class="chip" onclick="orderSheet('${p.symbol}','BUY','${p.prod}',${p.ltp})">Add</div><div class="chip" onclick="exitPos(${i})">Exit</div><div class="chip" onclick="reversePos(${i})">Reverse</div><div class="chip">SL</div><div class="chip">Target</div></div></div>`}).join('')}
function ordersHtml(){return state.orders.map(o=>`<div class="market-card row between"><div><div class="symbol">${o.symbol}</div><div class="exchange">${o.side} · ${o.qty} · ${o.type} · ${o.time}</div></div><div><div class="price">${o.price.toFixed(2)}</div><div class="green tiny">${o.status}</div></div></div>`).join('')}
function empty(t,s){return `<div class="empty"><div class="art">▤</div><h2>${t}</h2><div>${s}</div><div class="action-search" onclick="go('watch')">Search to Place Order ▶</div></div>`}
function exitPos(i){const p=state.positions[i];orderSheet(p.symbol,p.qty>0?'SELL':'BUY',p.prod,p.ltp)}
function reversePos(i){const p=state.positions[i];const q=Math.abs(p.qty)*2;orderSheet(p.symbol,p.qty>0?'SELL':'BUY',p.prod,p.ltp);setTimeout(()=>{const el=document.querySelector('#qty');if(el)el.value=q},50)}
function diary(){const wins=state.realized>0?1:0;return `<div class="app">${topbar(['Funds','Trader’s Diary','Controls'],'Trader’s Diary')}<main class="content"><div class="card pad"><div class="diaryStat"><div><b>${state.orders.length}</b><br><span class="tiny muted">Trades</span></div><div><b>${wins}</b><br><span class="tiny muted">Winning days</span></div><div><b class="${state.realized>=0?'green':'red'}">${fmt(state.realized)}</b><br><span class="tiny muted">Realized</span></div></div></div><div class="section-title">Automatic observations</div><div class="card pad"><p>• ${state.orders.length>state.limits.trades?'Overtrading alert: daily trade limit exceeded.':'Trade count is within your configured limit.'}</p><p>• ${state.orders.filter(o=>!o.sl).length} trades were placed without a defined stop loss.</p><p>• PaperTrade will add richer mistake tagging as the execution engine evolves.</p></div><div class="section-title">Trade log</div><div class="card">${state.diary.length?state.diary.map(d=>`<div class="listline"><span>${d.text}<br><span class="tiny muted">${d.time}</span></span><b>${d.tag}</b></div>`).join(''):'<div class="pad muted">No trades yet.</div>'}</div></main>${bottom()}</div>`}
function controls(){return `<div class="app">${topbar(['Funds','Trader’s Diary','Controls'],'Controls')}<main class="content"><div class="card"><div class="control row between"><div><b>Kill Switch</b><div class="tiny muted">Block all new paper orders</div></div><div class="switch ${state.kill?'on':''}" onclick="state.kill=!state.kill;save();render()"><i></i></div></div><div class="control row between"><div><b>Max daily loss</b><div class="tiny muted">Auto-stop threshold</div></div><input type="number" value="${state.limits.loss}" onchange="state.limits.loss=+this.value;save()"></div><div class="control row between"><div><b>Max trades / day</b><div class="tiny muted">Overtrading protection</div></div><input type="number" value="${state.limits.trades}" onchange="state.limits.trades=+this.value;save()"></div><div class="control row between"><div><b>Max open positions</b></div><input type="number" value="${state.limits.maxPos}" onchange="state.limits.maxPos=+this.value;save()"></div></div><div class="card pad"><b>Paper Trading Rule</b><p class="muted">These controls affect only PaperTrade. They do not change settings in any brokerage account.</p></div></main>${bottom()}</div>`}
function topup(){const v=Number(document.querySelector('#addMoney').value);if(v>0){state.cash+=v;save();render();toast('Virtual funds added') }}
function resetAccount(){if(confirm('Reset virtual orders, positions and P&L?')){state.positions=[];state.orders=[];state.diary=[];state.cash=1000000;state.used=0;state.realized=0;save();render()}}
function closeSheet(){S.innerHTML=''}
function toast(t){const d=document.createElement('div');d.className='toast';d.textContent=t;document.body.appendChild(d);setTimeout(()=>d.remove(),2400)}
function totalPnl(){return state.realized+state.positions.reduce((a,p)=>a+(liveLtp(p)-p.avg)*p.qty,0)}
function stockValue(){return state.positions.filter(p=>!p.symbol.includes(' CE')&&!p.symbol.includes(' PE')).reduce((a,p)=>a+Math.abs(p.avg*p.qty),0)}
function optionValue(){return state.positions.filter(p=>p.symbol.includes(' CE')||p.symbol.includes(' PE')).reduce((a,p)=>a+Math.abs(p.avg*p.qty),0)}
function liveLtp(p){const base=quoteFor(p.symbol);return base||p.ltp||p.avg}
function quoteFor(symbol){if(state.quotes[symbol])return state.quotes[symbol].p;if(symbol.startsWith('NIFTY ')){const m=symbol.match(/NIFTY\s+(\d+)\s+(CE|PE)/);if(m){const strike=+m[1],type=m[2],d=(strike-q('NIFTY').p)/50,time=Math.max(3,155-Math.abs(d)*5);return Math.max(1,(type==='CE'?Math.max(0,q('NIFTY').p-strike):Math.max(0,strike-q('NIFTY').p))+time)}}return q('NIFTY').p}
function tick(){Object.keys(state.quotes).forEach(k=>{const x=state.quotes[k],scale=k==='NIFTY'?3:k==='SENSEX'?8:k==='VIX'?.02:1;const r=(Math.random()-.49)*scale;x.p=Math.max(.1,x.p+r);x.c+=r;x.pc=(x.c/(x.p-x.c))*100});state.positions.forEach(p=>p.ltp=quoteFor(p.symbol));save();if(['watch','portfolio','orders','options','home'].includes(state.page)&&!S.innerHTML)render(false)}
function render(scroll=true){let html='';if(state.page==='home')html=home();else if(state.page==='watch')html=watch();else if(state.page==='portfolio')html=portfolio();else if(state.page==='orders')html=orders();else if(state.page==='funds')html=funds();else if(state.page==='options')html=options();else if(state.page==='chart')html=chartPage();A.innerHTML=html;if(state.page==='home')setTimeout(injectTicker,0);if(state.page==='chart')setTimeout(injectChart,0)}
window.go=go;window.topTab=topTab;window.openSymbol=openSymbol;window.chart=chart;window.flash=flash;window.orderSheet=orderSheet;window.closeSheet=closeSheet;window.sheetSide=sheetSide;window.placeFromSheet=placeFromSheet;window.exitPos=exitPos;window.reversePos=reversePos;window.topup=topup;window.resetAccount=resetAccount;window.state=state;window.render=render;
render();setInterval(tick,2500);
